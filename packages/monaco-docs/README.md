# monaco-docs
This is the docs built from the type declarations of `monaco-editor`.

`monaco` namespace can be imported in `intwc` with:
```typescript
import * as monaco from "@pistonite/intwc/monaco";
```
