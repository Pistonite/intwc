
version: '3'

includes:
  ecma:
    taskfile: ./node_modules/mono-dev/task/ecma.yaml
    internal: true
    optional: true

tasks:
  check:
    - task: ecma:check
  fix:
    - task: ecma:fix

  build:
    - task: pre-build
    - task: ecma:lib-build

  pre-build:
    - node --experimental-strip-types scripts/build-themes.ts
    - node --experimental-strip-types scripts/build-codicon.ts
    
  publish:
    - task: pre-build
    - task: ecma:fix
    - task: ecma:publish

